# Config file for air (hot reload)

# Working directory
root = "."
tmp_dir = "bin"

# Build settings
[build]
  cmd = "go build -buildvcs=false -o ./bin/boilerplate-one ./cmd/server"
  bin = "bin/boilerplate-one"
  full_bin = "./bin/boilerplate-one"
  include_ext = ["go"]
  exclude_dir = ["deploy", "bin"]
  exclude_file = []
  follow_symlink = true
  delay = 1000

# Log settings
[log]
  level = "debug"

# Command settings
[cmd]
  after_build = "chmod +x ./bin/boilerplate-one && echo '✅ Build completed!'"

# Watch settings
[watch]
  include_dir = ["."]
  exclude_dir = ["deploy", "bin"]
  poll_interval = 1000
